class IFC {
  setClient (client) {
    this.client = client;
  }

  setServer (server) {
    this.server = server;
  }

  setCrypto (crypto) {
    this.crypto = crypto;
  }

  setEvent (event) {
    this.event = event;
  }

  setSidechain (sidechain) {
    this.sidechain = sidechain;
  }
}

export default IFC;
